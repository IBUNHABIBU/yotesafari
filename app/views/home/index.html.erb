<% if @hero %>
  <!-- Hero Section -->
  <div class="relative h-screen min-h-[32rem] bg-gray-900 overflow-hidden">
    <!-- Background Image with Overlay -->
    <% if @hero.image.attached? %>
      <div class="absolute inset-0 z-0">
        <%= image_tag @hero.image, class: "w-full h-full object-cover opacity-70", alt: "Kilimarvellous Safari, kilimanjaro image" %>
        <div class="absolute inset-0 bg-gradient-to-r from-green-800/80 to-amber-800/50 mix-blend-multiply"></div>
      </div>
    <% end %>

    <!-- Content -->
    <div class="relative z-10 max-w-7xl mx-auto h-full flex items-center px-4 sm:px-6 lg:px-8">
      <div class="text-left max-w-3xl">
        <h1 class="text-4xl font-extrabold tracking-tight text-white sm:text-5xl lg:text-6xl">
          <span class="block"><%= @hero.title %></span>
          <span class="block text-amber-300"><%= @hero.subtitle %></span>
        </h1>
        <p class="mt-6 text-xl text-gray-100 max-w-lg">
          <%= @hero.description %>
        </p>
        <div class="mt-10 flex flex-wrap gap-4">
          <%= link_to "Explore Tours", "#featured-tours", class: "px-8 py-4 border border-transparent text-base font-medium rounded-md text-white bg-amber-600 hover:bg-amber-700 md:py-4 md:text-lg md:px-10 transition duration-300" %>
          <%= link_to "View Destinations", '#destinations', class: "px-8 py-4 border border-transparent text-base font-medium rounded-md text-white bg-green-800 hover:bg-green-700 md:py-4 md:text-lg md:px-10 transition duration-300" %>
        </div>
      </div>
    </div>

    <!-- Scrolling Indicator -->
    <div class="absolute bottom-10 left-1/2 transform -translate-x-1/2 z-10">
      <a href="#featured-tours" class="animate-bounce inline-flex items-center justify-center w-12 h-12 rounded-full bg-white/20 backdrop-blur-sm text-white hover:text-amber-300 transition">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </a>
    </div>
  </div>
<% else %>
  <!-- Empty State -->
  <div class="relative h-screen min-h-[32rem] bg-gray-900 flex items-center justify-center">
    <div class="text-center max-w-2xl px-4">
      <h2 class="text-3xl font-bold text-white">No hero section configured</h2>
      <p class="mt-4 text-lg text-gray-300">Get started by creating your travel hero section</p>
      <% if user_signed_in? %>
      <%= link_to "Create Hero Section", new_hero_path, class: "mt-6 inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-green-800 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-800" %>
   <% end %>
      </div>
  </div>
<% end %>


<!-- Featured Tours Section -->
<section id="featured-tours" class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
        <span class="block">Our Featured Adventures</span>
        <span class="block text-amber-600 mt-2">Handpicked Experiences</span>
      </h2>
      <p class="mt-4 max-w-2xl mx-auto text-xl text-gray-600">
        Discover our most popular tours loved by travelers worldwide
      </p>
    </div>

    <% if @featured_tours.any? %>
      <div class="grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3">
        <% @featured_tours.each do |tour| %>
          <div class="bg-white rounded-xl shadow-md overflow-hidden hover:shadow-xl transition duration-300">
            <% if tour.image.attached? %>
              <div class="h-48 overflow-hidden">
                <%= image_tag tour.image, class: "w-full h-full object-cover", alt: tour.name %>
              </div>
            <% else %>
              <div class="h-48 bg-gray-200 flex items-center justify-center">
                <span class="text-gray-500">No image available</span>
              </div>
            <% end %>
            <div class="p-6">
              <div class="flex items-center mb-2">
                <span class="inline-block px-2 py-1 text-xs font-semibold text-amber-800 bg-amber-100 rounded-full"><%= tour.category %></span>
                <span class="ml-auto text-sm font-medium text-green-800"><%= tour.duration %> days</span>
              </div>
              <h3 class="text-xl font-bold text-gray-900 mb-2"><%= tour.name %></h3>
              <p class="text-gray-600 mb-4"><%= truncate(tour.description, length: 100) %></p>
              <div class="flex items-center justify-between">
                <span class="text-lg font-bold text-green-800">$<%= tour.price %></span>
                <%= link_to "View Tour", travel_tour_path(tour), class: "px-4 py-2 text-sm font-medium text-white bg-amber-600 hover:bg-amber-700 rounded-md transition" %>
              </div>
            </div>
          </div>
        <% end %>
      </div>

      <div class="mt-12 text-center">
        <%= link_to "View All Tours", travel_tours_path, class: "px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-green-800 hover:bg-green-700 shadow-sm" %>
      </div>
    <% else %>
      <div class="text-center py-8">
        <p class="text-gray-500 mb-4">No featured tours available yet</p>
        <% if user_signed_in? %>
          <%= link_to "Create a Tour", new_travel_tour_path, class: "px-4 py-2 bg-green-800 text-white rounded-md hover:bg-green-700" %>
        <% end %>
      </div>
    <% end %>
  </div>
</section>

<!-- Rest of your home page sections -->
<section class="py-16 bg-neutral-light">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="lg:text-center">
      <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
        Why Travel With Us?
      </h2>
      <p class="mt-4 max-w-2xl mx-auto text-xl text-gray-600">
        We're committed to making your adventure unforgettable
      </p>
    </div>

    <div class="mt-16 grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-4">
      <div class="text-center">
        <div class="mx-auto h-16 w-16 flex items-center justify-center bg-green-100 rounded-full text-green-800">
          <svg class="h-8 w-8" fill="currentColor" viewBox="0 0 20 20"><path d="M9 6a3 3 0 11-6 0 3 3 0 016 0zM17 6a3 3 0 11-6 0 3 3 0 016 0zM12.93 17c.046-.327.07-.66.07-1a6.97 6.97 0 00-1.5-4.33A5 5 0 0119 16v1h-6.07zM6 11a5 5 0 015 5v1H1v-1a5 5 0 015-5z"/></svg>
        </div>
        <h3 class="mt-4 text-lg font-medium text-gray-900">Expert Guides</h3>
        <p class="mt-2 text-gray-600">Our local guides have unmatched knowledge of each destination.</p>
      </div>
      
      <div class="text-center">
        <div class="mx-auto h-16 w-16 flex items-center justify-center bg-amber-100 rounded-full text-amber-800">
          <svg class="h-8 w-8" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M6 6V5a3 3 0 013-3h2a3 3 0 013 3v1h2a2 2 0 012 2v3.57A22.952 22.952 0 0110 13a22.95 22.95 0 01-8-1.43V8a2 2 0 012-2h2zm2-1a1 1 0 011-1h2a1 1 0 011 1v1H8V5zm1 5a1 1 0 011-1h.01a1 1 0 110 2H10a1 1 0 01-1-1z" clip-rule="evenodd"/><path d="M2 13.692V16a2 2 0 002 2h12a2 2 0 002-2v-2.308A24.974 24.974 0 0110 15c-2.796 0-5.487-.46-8-1.308z"/></svg>
        </div>
        <h3 class="mt-4 text-lg font-medium text-gray-900">Small Groups</h3>
        <p class="mt-2 text-gray-600">Intimate groups for a more personal experience.</p>
      </div>
      
      <div class="text-center">
        <div class="mx-auto h-16 w-16 flex items-center justify-center bg-green-100 rounded-full text-green-800">
          <svg class="h-8 w-8" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd"/></svg>
        </div>
        <h3 class="mt-4 text-lg font-medium text-gray-900">Unique Locations</h3>
        <p class="mt-2 text-gray-600">Access to hidden gems beyond typical tourist routes.</p>
      </div>
      
      <div class="text-center">
        <div class="mx-auto h-16 w-16 flex items-center justify-center bg-amber-100 rounded-full text-amber-800">
          <svg class="h-8 w-8" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm3.293-7.707a1 1 0 011.414 0L9 10.586V3a1 1 0 112 0v7.586l1.293-1.293a1 1 0 111.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd"/></svg>
        </div>
        <h3 class="mt-4 text-lg font-medium text-gray-900">Easy Booking</h3>
        <p class="mt-2 text-gray-600">Simple online reservation with flexible options.</p>
      </div>
    </div>
  </div>
</section>

<!-- After Why Choose Us section -->

<!-- Destination Highlights Section -->
<section class="py-16 bg-white" id="destinations">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
        Explore Our Top Destinations
      </h2>
      <p class="mt-4 max-w-2xl mx-auto text-xl text-gray-600">
        From majestic mountains to pristine beaches
      </p>
    </div>

    <% if @popular_destinations.any? %>
      <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
        <% @popular_destinations.each do |destination| %>
          <div class="relative group overflow-hidden rounded-lg shadow-md h-96">
            <% if destination.image.attached? %>
              <%= image_tag destination.image, class: "w-full h-full object-cover transform group-hover:scale-105 transition duration-500", alt: destination.name %>
            <% else %>
              <div class="w-full h-full bg-gray-200 flex items-center justify-center">
                <span class="text-gray-500">No image available</span>
              </div>
            <% end %>
            <div class="absolute inset-0 bg-gradient-to-t from-gray-900/70 via-gray-900/30 to-transparent"></div>
            <div class="absolute bottom-0 left-0 p-6 w-full">
              <h3 class="text-2xl font-bold text-white"><%= destination.name %></h3>
              <p class="mt-2 text-gray-200"><%= destination.country %></p>
              <%= link_to "View destination", destination_path(destination), class: "mt-4 inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-green-800 bg-amber-300 hover:bg-amber-400" %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-8">
        <p class="text-gray-500 mb-4">No featured destinations available yet</p>
        <% if user_signed_in? %>
          <%= link_to "Add Destinations", new_destination_path, class: "px-4 py-2 bg-green-800 text-white rounded-md hover:bg-green-700" %>
        <% end %>
      </div>
    <% end %>
  </div>
</section>

<!-- Testimonials Section -->
<section class="py-16 bg-neutral-light">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
        Traveler Stories
      </h2>
      <p class="mt-4 max-w-2xl mx-auto text-xl text-gray-600">
        Don't just take our word for it
      </p>
    </div>

    <% if @testimonials.any? %>
      <div class="grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-3">
        <% @testimonials.each do |testimonial| %>
          <div class="bg-white p-6 rounded-lg shadow-md">
            <div class="flex items-center mb-4">
              <% if testimonial.avatar.attached? %>
                <%= image_tag testimonial.avatar, class: "h-12 w-12 rounded-full object-cover", alt: testimonial.author_name %>
              <% else %>
                <div class="h-12 w-12 rounded-full bg-gray-200 flex items-center justify-center">
                  <span class="text-gray-500 text-xs">Photo</span>
                </div>
              <% end %>
              <div class="ml-4">
                <h4 class="text-lg font-medium text-gray-900"><%= testimonial.author_name %></h4>
                <% if testimonial.author_title.present? %>
                  <p class="text-amber-600 text-sm"><%= testimonial.author_title %></p>
                <% end %>
              </div>
            </div>
            <p class="text-gray-600 italic">"<%= testimonial.content %>"</p>
            <div class="mt-4 flex">
              <span class="text-amber-400"><%= testimonial.star_rating %></span>
            </div>
          </div>
        <% end %>
      </div>

      <div class="mt-12 text-center">
        <%= link_to "Read More Stories", testimonials_path, class: "px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-green-800 hover:bg-green-700 shadow-sm" %>
      </div>
    <% else %>
      <div class="text-center py-8">
        <p class="text-gray-500 mb-4">No testimonials yet</p>
        
          <%= link_to "Add Testimonial", new_testimonial_path, class: "px-4 py-2 bg-green-800 text-white rounded-md hover:bg-green-700" %>
        
      </div>
    <% end %>
  </div>
</section>

<!-- Call to Action Section -->
<section class="bg-green-800">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 text-center">
    <h2 class="text-3xl font-extrabold text-white sm:text-4xl">
      <span class="block">Ready for Your Next Adventure?</span>
    </h2>
    <p class="mt-4 max-w-2xl mx-auto text-xl text-green-100">
      Join thousands of happy travelers who've explored with us
    </p>
    <div class="mt-8 flex flex-col sm:flex-row justify-center space-y-4 sm:space-y-0 sm:space-x-4">
      <%= link_to "Book Now", new_booking_path, class: "px-8 py-3 border border-transparent text-base font-medium rounded-md text-green-800 bg-amber-300 hover:bg-amber-400 md:py-4 md:text-lg md:px-10 transition duration-300" %>
      <%= link_to "Contact Us", pages_contact_path, class: "px-8 py-3 border border-transparent text-base font-medium rounded-md text-white bg-green-700 hover:bg-green-600 md:py-4 md:text-lg md:px-10 transition duration-300" %>
    </div>
  </div>
</section>

